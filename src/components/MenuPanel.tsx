import type { Component } from "solid-js";

const MenuPanel: Component = (props) => {
	return (
		<div class="hidden size-full min-h-screen min-w-72 grow items-start justify-end border-r border-black lg:flex">
			HELLO
		</div>
	);
};

export default MenuPanel;
